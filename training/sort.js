const lists = [3,1,7,0,17]
console.log(lists)

// sort in descending order 
lists.sort((a,b) => {return b-a})
console.log(lists)

// sort in ascending order
lists.sort((a, b) => { return a - b })
console.log(lists)